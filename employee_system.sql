CREATE DATABASE employee_system;

use employee_system;

CREATE TABLE employee_info  (
     id            VARCHAR(50),
     name          VARCHAR(50),
     address       VARCHAR(50),
     age           INT,
     salary        INT,
     phone         INT
  );